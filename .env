# ──────────────────────────────────────────────────────────
# LitVM Token Deployer — Environment Configuration
# ──────────────────────────────────────────────────────────

# Fee wallet address that receives 10,000 tokens on every deployment.
# Must be a valid Ethereum/EVM address (0x-prefixed, 40 hex chars).
# ethers.getAddress() is used at startup to checksum-validate this value.
VITE_FEE_WALLET=0x896C20Da40c2A4df9B7C98B16a8D5A95129161a5
